<div>
  <% @items.each do |item| %>
    <div class="col-md-3 text-center item-container">
      <%= image_tag(item.image_url, class: 'item-image') %>
      <h3><%= item.name %></h3>
    </div>
  <% end %>
</div>
